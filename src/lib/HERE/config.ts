export const HEREconfig = {
    routesEndpoint: import.meta.env.VITE_HERE_ROUTE_ENDPOINT,
    hereApiKey: import.meta.env.VITE_HERE_API_KEY,
    geocodeEndpoint: import.meta.env.VITE_HERE_GEOCODE_ENDPOINT,
}

/* 
 WORKFLOW:
  we re going to add all the endpoints and secret stuff exposed in here 
  NOTE: 
    to be consumed by the api f().
*/

